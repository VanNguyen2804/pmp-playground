# Local development defaults to http://localhost:8080/api when this is omitted.
# Render production: use the PUBLIC backend URL. /api is appended automatically.
API_BASE_URL=https://pmp-playground-api.onrender.com
